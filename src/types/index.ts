type CodemodOptions = {
  projectRoot: string;
  projectType: 'app' | 'v1-addon' | 'v2-addon';
};

type Options = {
  projectRoot: string;
  projectType: 'app' | 'v1-addon' | 'v2-addon';
};

export type { CodemodOptions, Options };
